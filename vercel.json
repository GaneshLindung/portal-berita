{
  "version": 2,
  "installCommand": "if [ -d news-portal-frontend ]; then cd news-portal-frontend; fi && npm install",
  "buildCommand": "if [ -d news-portal-frontend ]; then cd news-portal-frontend; fi && npm run build",
  "builds": [
    {
      "src": "news-portal-frontend/next.config.mjs",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
